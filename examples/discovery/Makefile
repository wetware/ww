# Discovery — Greeter vat service with service-name peer discovery
#
# Usage: make -C examples/discovery

ROOT := ../..

.PHONY: all clean

all:
	$(MAKE) -C $(ROOT) p3-discovery

clean:
	rm -f bin/discovery.wasm
