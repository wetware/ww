# Chess engine — Cap'n Proto vat service with service-name discovery
#
# Usage: make -C examples/chess

ROOT := ../..

.PHONY: all clean

all:
	$(MAKE) -C $(ROOT) p3-chess

clean:
	rm -f bin/chess-demo.wasm
