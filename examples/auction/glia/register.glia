; Demo snippet: register auction on RPC + HTTP transports.
(def auction (cell (load "bin/auction.wasm")))

(perform host :listen :vat "auction" auction)
(perform host :listen :http "/auction" auction)
