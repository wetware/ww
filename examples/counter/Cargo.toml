[package]
name = "counter"
version = "0.1.0"
edition = "2021"

[workspace]  # standalone — not part of the host workspace

[lib]
crate-type = ["cdylib"]

[dependencies]
wagi-guest = { path = "../../crates/wagi-guest" }
