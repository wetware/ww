[package]
name = "wagi-guest"
version = "0.1.0"
edition = "2021"
description = "Thin helper for writing WAGI (CGI-over-WASI) guest cells"

[workspace]  # standalone — not part of the host workspace (targets native wasm32-wasip3)

[dependencies]
wit-bindgen = "=0.61.1"
